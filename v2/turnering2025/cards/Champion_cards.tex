\renewcommand{\frfoot}{ Champion }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      all & As unit it came from \\
slow & [A(fast, fly), Chase, Chase] \\
fast fly & [Chase, B(slow, land), -] \\

  \end{tabular}
}{ Champion \\ Movement }

\renewcommand{\frfoot}{ Champion }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      all & As unit it came from \\

  \end{tabular}
}{ Champion \\ Fire }

