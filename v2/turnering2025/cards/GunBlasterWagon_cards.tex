\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, F, -] \\
slow & [L, -, -] \\
still & [L, -, -] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, R, -] \\
slow & [L, L, -] \\
still & [L, L, -] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, L, -] \\
slow & [R, -, -] \\
still & [L, L, L] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, B, -] \\
slow & [R, R, -] \\
still & [R, -, -] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [B, B, -] \\
slow & [A, F, -] \\
still & [R, R, -] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, B, B] \\
slow & [B, -, -] \\
still & [R, R, R] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, F, R] \\
slow & [F, -, -] \\
still & [-, -, -] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, F, L] \\
slow & [B, rev, -] \\
still & [R, A, F] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, F, F+R] \\
still & [L, A, F] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, F, F+L] \\
still & [A, F, -] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, F, F] \\
still & [rev, -, -] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      still & [A+A, F, F] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      still & [A+A+L, F, F] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      still & [A+A+R, F, F] \\

  \end{tabular}
}{ GunBlasterWagon \\ Movement }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [Fire, Load] \\
slow & [-, Load] \\
fast & [-, Load] \\

  \end{tabular}
}{ GunBlasterWagon \\ Fire }

\renewcommand{\frfoot}{ GunBlasterWagon }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [Load, -] \\
slow & [Load, -] \\
fast & [Load, -] \\

  \end{tabular}
}{ GunBlasterWagon \\ Fire }

