\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [Chase, -, -] \\
still & [360°, -, -] \\
crawl & [360°, F, -] \\

  \end{tabular}
}{ Scout \\ Movement }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [-, -, Chase] \\
still & [360°, A, F] \\
crawl & [360°, B[still], -] \\

  \end{tabular}
}{ Scout \\ Movement }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [360°, F, 360°] \\
still & [360°, A[crawl], F] \\
crawl & [360°, A[slow], F] \\

  \end{tabular}
}{ Scout \\ Movement }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [360°, 360°, 360°] \\

  \end{tabular}
}{ Scout \\ Movement }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [360°, F, B] \\

  \end{tabular}
}{ Scout \\ Movement }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [360°, B[crawl], -] \\

  \end{tabular}
}{ Scout \\ Movement }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      
  \end{tabular}
}{ Scout \\ Fire }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [Load, -] \\

  \end{tabular}
}{ Scout \\ Fire }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [-, Load] \\

  \end{tabular}
}{ Scout \\ Fire }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [Fire, -] \\

  \end{tabular}
}{ Scout \\ Fire }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [-, Fire] \\

  \end{tabular}
}{ Scout \\ Fire }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [Aim, -] \\

  \end{tabular}
}{ Scout \\ Fire }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [-, Aim] \\

  \end{tabular}
}{ Scout \\ Fire }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [Spot, Spot] \\

  \end{tabular}
}{ Scout \\ Fire }

\renewcommand{\frfoot}{ Scout }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      slow & [Spot, Spot] \\

  \end{tabular}
}{ Scout \\ Fire }

