\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [Load, -] \\
slow & [Load, -] \\
fast & [Load, -] \\

  \end{tabular}
}{ HammerHead \\ Fire }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Fire }

\card{\begin{tabular}{ll}
      still & [Fire, -] \\
slow & [Fire, -] \\
fast & [Fire, -] \\

  \end{tabular}
}{ HammerHead \\ Fire }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, F, -] \\
slow & [L, -, -] \\
still & [L, -, -] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, R, -] \\
slow & [L, L, -] \\
still & [L, L, -] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, L, -] \\
slow & [R, -, -] \\
still & [L, L, L] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [F, B, -] \\
slow & [R, R, -] \\
still & [R, -, -] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      fast & [B, B, -] \\
slow & [A, F, -] \\
still & [R, R, -] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [B, -, -] \\
still & [R, R, R] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [F, -, -] \\
still & [-, -, -] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      slow & [B, rev, -] \\
still & [R, A, F] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      still & [L, A, F] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      still & [A, F, -] \\

  \end{tabular}
}{ HammerHead \\ Movement }

\renewcommand{\frfoot}{ HammerHead }
\renewcommand{\flhead}{ Movement }

\card{\begin{tabular}{ll}
      still & [rev, -, -] \\

  \end{tabular}
}{ HammerHead \\ Movement }

