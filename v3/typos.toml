[files]
# card_standar.tex is a legacy flacards sample kept for reference; its filename
# misspelling is not ours to rename (superseded by templates/latex/cards/).
extend-exclude = [
    "rules/todo.md",
    "templates/card_standar.tex",
    # Third-party ComfyUI exports: the embedded positive-prompt text is data the
    # Service overwrites at generate time, not prose we maintain.
    "workflows/cloud/*.json",
    "workflows/examples/*.json",
    # An ADR about a typo will always contain that typo.
    "docs/adr/0011-coverage-survey-of-assets.md",
    "docs/specials-census.md",
    # Generated army data: the Nicks in here are player-chosen names, and
    # `just spell-fix` would rewrite them in place, desyncing the JSON from the
    # build script that generates it. The build scripts stay spell-checked.
    # Both shapes are needed: `*` does not cross a `/`, and armies live both
    # directly in `armies/` and one level down in `armies/<tournament>/`.
    "armies/*.json",
    "armies/**/*.json",
]

[default]
# Ignore any line ending with `# typos: ignore`
extend-ignore-re = ["(?Rm)^.*(#|//)\\s*typos:\\s*ignore$"]
locale = "en-us"

[default.extend-words]
# Domain misspellings the default `typos` dictionary does not catch. `just spell`
# flags them; `just spell-fix` rewrites every occurrence repo-wide, including
# inside identifier keys — run `just check` afterward to confirm nothing desynced.
#
# Matching is word-level, *after* typos splits on `_` and camelCase. So `cracle`
# never reaches `tentacle_craclespears`: `craclespears` is one token there. Hence
# exact-token entries alongside a base word — they are not duplicates, and
# deleting one as redundant silently stops guarding the compound.
#
# `extend-words` also rejects the multi-correction array form, so there is no
# flag-but-do-not-fix option; leave uncertain candidates out entirely.
abomiantion = "abomination"
accurazy = "accuracy"
addion = "addition"
alot = "a lot"
amix = "a mix"
ammor = "ammo"
andr = "and"
anothes = "another"
armred = "armed"
arngle = "angle"
atleast = "at least"
axe = "axe" # Allowed name
bajonet = "bayonet"
bajonett = "bayonet"
ballon = "balloon"
barrled = "barreled"
bost = "boost"
braodside = "broadside"
brazz = "brass"
camoufage = "camouflage"
camufloulage = "camouflage"
caos = "chaos"
cloacking = "cloaking"
cload = "cloud"
complettness = "completeness"
cracle = "crackle"
craclespears = "cracklespears"
craftmanship = "craftsmanship"
crittical = "critical"
dager = "dagger"
deacellerates = "decelerates"
depoloy = "deploy"
destroid = "destroyed"
dinaosour = "dinosaur"
dinosour = "dinosaur"
dinosours = "dinosaurs"
disensamble = "disassemble"
effectd = "affected"
elecant = "elegant"
elfs = "elves"
endurence = "endurance"
enemis = "enemies"
esscape = "escape"
exacatly = "exactly"
exceel = "excel"
eys = "eyes"
fanzy = "fancy"
firng = "firing"
flexibale = "flexible"
flys = "flies"
forrest = "forest"
furrious = "furious"
futureistick = "futuristic"
gadgdets = "gadgets"
gadgedts = "gadgets"
gonome = "gnome"
gigant = "giant"
gunnrey = "gunnery"
healin = "healing"
hexe = "hex"
hippotpotamus = "hippopotamus"
humandoid = "humanoid"
hyptnotized = "hypnotized"
immmune = "immune"
immun = "immune"
impassble = "impassable"
imune = "immune"
imunity = "immunity"
infangry = "infantry"
infantery = "infantry"
infantri = "infantry"
infront = "in front"
insantiy = "insanity"
meca = "mecha"
monstosity = "monstrosity"
motorcyle = "motorcycle"
multibarrled = "multibarreled"
mysteri = "mystery"
napleonic = "napoleonic"
neighbohring = "neighboring"
neutrilize = "neutralize"
normel = "normal"
ordes = "orders"
outfints = "outfits"
overcroweded = "overcrowded"
pachephalosourus = "pachycephalosaurus"
pachyephalosaurus = "pachycephalosaurus"
pachyephalosourus = "pachycephalosaurus"
plotform = "platform"
pluss = "plus"
porly = "poorly"
psychcic = "psychic"
psycic = "psychic"
reatreat = "retreat"
reistance = "resistance"
repear = "repair"
sadle = "saddle"
saouropod = "sauropod"
schaken = "shaken"
shrapnell = "shrapnel"
simulatiously = "simultaneously"
simultainiously = "simultaneously"
simultainiusly = "simultaneously"
simultaniuously = "simultaneously"
simultaniusly = "simultaneously"
# Norwegian, kept because the game designer's notes are preserved verbatim
# inside `todo` fields, where a `# typos: ignore` marker cannot reach.
som = "som"
souropod = "sauropod"
spesial = "special"
spesialization = "specialization"
spitt = "spit"
suptracked = "subtracted"
swards = "swords"
tentancles = "tentacles"
theeth = "teeth"
trapp = "trap"
thematicly = "thematically"
thougher = "tougher"
thoughness = "toughness"
towes = "tows"
tranee = "trainee"
treatet = "treated"
trueth = "truth"
unoccopied = "unoccupied"
unsuaual = "unusual"
upt = "up"
veilding = "wielding"
victorypoints = "victory points"
vitory = "victory"
wapon = "weapon"
weaing = "wearing"
wreking = "wrecking"
